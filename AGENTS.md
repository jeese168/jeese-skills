# AGENTS.md

本文件适用于整个仓库。它只保存长期稳定、会影响 Agent 决策的项目约定；项目介绍、当前内容和使用方式见 `README.md`。

## 长期目标

`jeese-skills` 把个人需要的理解、组织、解释和表达方式从具体模型中抽离出来，沉淀为可控制、可验证、可跨平台迁移的 Agent Skills。

本仓库负责个人交互适配，包括 Agent 怎样建立理解坐标、组织信息、解释判断、交付修改和形成文档。通用的软件工程规划、实现、测试、审查和迭代流程优先交给已经成熟的方法和工具；本仓库可以适配这些流程产生的交互和输出，但不复制一套新的工程工作流。

只有通用能力无法覆盖、并且经过真实协作验证的个人适配，才应进入本仓库；不要把仓库扩张成通用最佳实践百科或提示词收藏夹。

## 唯一真源与可迁移性

- `skills/<skill-name>/` 是每个 Skill 的唯一可编辑源码。平台安装目录、Plugin、压缩包和上传产物都不能成为第二份源码。
- `SKILL.md` 是每个 Skill 的必需入口。运行所需的 references、scripts 和 assets 必须位于 Skill 根目录内；不需要的目录不创建。
- 可以使用指向 Skill 内部文件的相对路径；不得依赖本机绝对路径、跳出 Skill 根目录的相对路径、外部软链接，或只存在于某台电脑上的原始材料。
- 原始语料可以用于蒸馏规则，但运行时 Skill 必须携带已经提炼出的规则和必要例子，不能要求目标机器重新访问原始语料。
- 需要平台专属元数据时，放在 Skill 内的 `agents/`。没有真实分发需求时，不创建 Plugin、平台专用副本或占位目录。

## 信息边界与校准

- 仓库保存可复用的协作能力，不保存原始聊天、私人经历、账号信息或未经筛选的个人资料，除非用户明确要求保存某份材料。
- 区分可验证事实、语料观察、用户明确表达的偏好和 Agent 推断；不要把推断写成用户规则，也不要把风格语料当成技术事实来源。
- 描述仓库中已经存在的文件、目录、规则或职责时，使用仓库实际定义的名称，不用未经定义的新术语或近义抽象代替已有概念。确需引入新概念时，先给出定义，说明它对应的实际对象、与已有概念的关系，以及现有名称为什么不足。
- 一次生成结果的修改只是校准证据。先判断它是当前任务要求、题材局部规则、跨任务稳定习惯，还是可以交给脚本的机械问题。
- 不因单个反例增加无条件禁令。优先修改导致问题的最小规则，再用真实任务验证是否值得扩大范围。

## Skill 边界与渐进式披露

- 平台先根据 Skill 的 `name` 和 `description` 选择 Skill；Skill 激活后，Agent 再按照 `SKILL.md` 的路由读取内部 references。
- 如果多个变体共享同一主要任务、工作过程和验收标准，只把差异放进 references。
- 如果用户触发方式、主要产物、工作过程或验收标准明显不同，才拆成独立 Skills。
- 只有一个任务确实需要固定的多阶段方法时，才创建工作流 Skill。不要建立只负责给其他 Skills 分类的总路由。
- 每个独立 Skill 保持自包含，不依赖另一个 Skill 必须同时激活。

## 修改与验证

修改前完整阅读目标 `SKILL.md`，并按它的路由读取与任务相关的 references。创建或大幅修改 Skill 时，使用可用的 `skill-creator` 指南；普通修正保持最小范围。

完成后至少检查：

1. Skill 结构、frontmatter、目录名与 `description` 是否一致；
2. 所有运行依赖是否位于 Skill 根目录内，引用是否存在且可迁移；
3. 新增或修改的脚本是否实际运行，行为是否经过代表性任务验证；
4. `git diff --check` 和完整差异中是否存在无关改动。

只在用户明确要求时提交、推送、发布、安装或修改仓库外的平台配置。
